{% extends 'template.html.twig' %}
{% use 'bootstrap_3_layout.html.twig' %}

{% block body %}
  <div class="row">
    <div class="col-sm-6">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">About Team Types</h3>
        </div>
        <div class="panel-body">
          <p><i class="fa fa-users fa-border fa-2x" style="text-shadow: 1px 1px #ccc;" aria-hidden="true"></i>&nbsp;<span class="lead">Family</span> <sup style="color:red;">*Most Popular!</sup></p>
          <p>Family teams are for parents who want a easy way to collect donations for a school campaign without a lot of work. All donations are split evenly amongst all children and their classes.<p>
          <hr>
          <p><i class="fa fa-apple fa-border fa-2x" style="text-shadow: 1px 1px #ccc;" aria-hidden="true"></i>&nbsp;<span class="lead">Teacher/Class</span></p>
          <p>Teacher/Class teams are for collecting donations for an entire class. Great for teachers to do fundraising!<p>
          <hr>
          <p><i class="fa fa-graduation-cap fa-border fa-2x" style="text-shadow: 1px 1px #ccc;" aria-hidden="true"></i>&nbsp;<span class="lead">Student</span></p>
          <p>Student teams are for collecting donations for a single (1) student. Great for individuals or competitive siblings.<p>
        </div>
      </div>
    </div>
    <div class="col-sm-6">

      {% set campaignUrl = null %}
      {% set campaignUrl = app.request.attributes.get('_route_params')['campaignUrl'] %}

      <p style="text-align:left;">
        Now lets create your team page!
        <hr/>
        {% if error is defined %}
          <p class="well-sm bg-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</p>
        {% endif %}

        <form name="user" method="post" class="form-horizontal">
          <div class="form-group">
            <label class="col-sm-4 control-label required" for="team_teamType">Team Type</label>
            <div class="col-sm-6">
              <select id="team_teamType" name="team[teamType]" required="required" class="form-control">
                {% for teamType in teamTypes %}
                  <option value="{{teamType.value}}" {% if teamType.value == "family" %} selected="selected" {% endif %}>{{teamType.name}}</option>
               {% endfor %}
              </select>
            </div>
          </div>
          <div class="form-group">
            <label class="col-sm-4 control-label required" for="user_firstName">Team Name</label>
            <div class="col-sm-6"><input type="text" id="team_name" required="required" name="team[name]" class="form-control"></div>
          </div>
          <hr>
          <div class="form-group">
            <div class="col-sm-12">
              <button type="submit" id="submit" class="btn btn-primary">Go to step 3 <i class="fa fa-arrow-right" aria-hidden="true"></i></button>
            </div>
          </div>

          <input type="hidden" id="user_defaultCampaignId" name="user[defaultCampaignId]"><input type="hidden" id="user__token" name="user[_token]" value="oPf8W_BVWF3f9zvbETkokwPUnKXWK_I9HYOGSyt5Thk">
        </form>

      </div>
    </div>
  {% endblock body %}

  {% block javascript %}
    <script type="text/javascript">
      $('#user_terms_of_service').click(function() {
        if ($(this).attr('checked') == false) {
          $('#submit').attr("disabled", "disabled");
        } else {
          $('#submit').removeAttr('disabled');
        }
      });
    </script>
  {% endblock %}
